- package: {{cookiecutter.repository_name}}-package

- model: {{cookiecutter.repository_name}}-model
  sourcecode:
    - '*.py'
    - guild.yml
    - exclude:
        dir:
          - input
          - tb
          - checkpoints
  operations:
    prepare:
      main: prepare
      flags-import: all
      output-scalars: off
    search_lr:
      main: search_lr
      flags-import: all
      output-scalars: off
      requires:
        - prepare
        - train_split
    train:
      main: train
      flags-import: all
      output-scalars: off
      requires:
        - prepare
        - train_split
    retrain:
      main: train
      flags-import: all
      output-scalars: off
      requires:
        - model
        - prepare
        - train_split
    evaluate:
      main: evaluate
      flags-import: all
      output-scalars: off
      requires:
        - model
        - prepare
        - train_split
  resources:
    model:
      name: model
      path: model
      sources:
        - operation: train|retrain
    prepare:
      name: prepare
      path: prepare
      sources:
        - operation: prepare
    train_split:
      path: {{cookiecutter.package_name}}/data
      sources:
        - {{cookiecutter.package_name}}/data
